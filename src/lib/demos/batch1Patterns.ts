import type { DemoRecipe } from './recipes'
import { dialogFocusLoop } from './batch1Dialog'

const base = `main{display:block;background:#edf0f6;color:#202b42;container-type:inline-size}.workspace{height:100%;overflow:auto;padding:clamp(16px,4cqw,40px)}.eyebrow{font:10px monospace;letter-spacing:.15em;color:#65718a}.workspace h1{font-size:clamp(26px,5cqw,46px);margin:12px 0}.workspace p{font-size:13px;line-height:1.7;color:#62708a}.workspace button,dialog button{min-height:40px;padding:10px 14px;border:1px solid #d1d7e2;border-radius:9px;background:white;color:#27364d;font:inherit;font-size:12px}.workspace .primary{background:#4658cc;color:white;border-color:#4658cc}.workspace button:focus-visible,dialog :focus-visible{outline:3px solid #596bdd;outline-offset:2px}.toolbar{display:flex;gap:8px;flex-wrap:wrap;margin:18px 0}.workspace h2,dialog h2{font-size:18px;margin:10px 0}dialog{z-index:10;color:#202b42;background:#fff;border:1px solid #d6dce7;border-radius:16px;box-shadow:0 20px 70px #1d294d38;padding:18px;max-height:94%;overflow:auto}dialog::backdrop{background:#17223b66;backdrop-filter:blur(2px)}dialog p{font-size:12px;line-height:1.6}.close{float:right}dialog:not([open]){display:none}@container(max-width:440px){.workspace{padding:16px}.workspace h1{font-size:26px}.supporting{display:none}.toolbar{margin:12px 0}.workspace p{font-size:11px}}`
// The resting preview is non-modal and never takes focus from the surrounding catalog.
// Explicit open enters native modal behavior, including inert background and focus containment.
const modal = `const dialog=document.querySelector('dialog'),opener=document.querySelector('.open-dialog');function openDialog(){if(dialog.open)dialog.close();dialog.showModal();}opener.onclick=openDialog;document.querySelector('.close').onclick=()=>dialog.close();dialog.addEventListener('close',()=>opener.focus({preventScroll:true}));dialog.addEventListener('click',e=>{const r=dialog.getBoundingClientRect();if(e.target===dialog&&(e.clientX<r.left||e.clientX>r.right||e.clientY<r.top||e.clientY>r.bottom))dialog.close()});`

export const batch1PatternRecipes: Record<string,DemoRecipe> = {
 'pattern-bottom-sheet': {
  html:`<div class="workspace"><small class="eyebrow">STUDIO / RESERVATION</small><h1>다음 작업 공간</h1><p class="supporting">햇살이 드는 창가에서 아이디어를 펼쳐보세요.</p><button class="primary open-dialog">예약 옵션 열기 ↑</button><div class="map" aria-hidden="true"><i></i><span>STUDIO 04</span></div></div><dialog open aria-labelledby="sheet-title" data-snap="65"><button class="handle" aria-label="시트 높이 조절: 위아래 화살표" role="slider" aria-valuemin="40" aria-valuemax="90" aria-valuenow="65" aria-valuetext="65%">―</button><button class="close" aria-label="닫기">×</button><h2 id="sheet-title">창가 스튜디오</h2><p class="summary">자연광 · 2–4인 · 시간당 ₩18,000</p><div class="snap-controls"><button data-size="40">작게</button><button data-size="65">중간</button><button data-size="90">크게</button></div><div class="sheet-content"><h3>예약 시간</h3><fieldset><legend>시작 시간 선택</legend><label><input type="radio" name="time" value="10:00" checked> 10:00</label><label><input type="radio" name="time" value="14:00"> 14:00</label><label><input type="radio" name="time" value="17:00"> 17:00</label></fieldset><p>넓은 테이블, 무선 인터넷과 차 한 잔이 준비되어 있습니다.</p><button class="reserve">이 시간 선택</button><p class="selection" role="status"></p></div></dialog>`,
  css:base+`.map{height:200px;margin-top:22px;background:repeating-linear-gradient(30deg,transparent 0 44px,#fff 45px 53px),repeating-linear-gradient(120deg,#dce7dd 0 50px,#fff 51px 60px);border-radius:16px;display:grid;place-content:center;text-align:center;font:10px monospace}.map i{width:22px;height:22px;border-radius:50% 50% 50% 0;background:#4658cc;rotate:-45deg;margin:0 auto 14px}dialog{position:absolute;inset:auto 0 0;width:min(100%,540px);max-width:100%;height:65%;max-height:95%;margin:0 auto;border-radius:22px 22px 0 0;padding:12px 20px 20px;transition:height 240ms ease;overflow:hidden;display:flex;flex-direction:column}.handle{touch-action:none;cursor:ns-resize;align-self:center;width:80px;min-height:24px;padding:0!important;border:0!important;color:#8f99ae!important;font-size:30px!important;line-height:20px}.close{position:absolute;right:12px;top:10px}.summary{margin:0 0 10px}.snap-controls{display:flex;gap:6px}.snap-controls button{padding:5px 12px;min-height:32px}.snap-controls button[aria-pressed=true]{background:#e7ebff;color:#3648b8}.sheet-content{overflow:auto;min-height:0;margin-top:8px}.sheet-content h3{font-size:14px}.sheet-content fieldset{display:flex;flex-wrap:wrap;gap:14px;border:1px solid #d6dce7;border-radius:8px;padding:12px;font-size:12px}.reserve{background:#4658cc;color:white;margin-top:10px}.selection{min-height:20px}@container(max-width:440px){dialog{padding:6px 14px 14px}dialog h2{font-size:16px;margin:4px 0}.summary{font-size:10px}.handle{min-height:18px}.snap-controls button{min-height:28px;padding:3px 10px}.close{min-height:28px;top:6px}.sheet-content{margin-top:3px}}`,
  js:modal+dialogFocusLoop+`const handle=document.querySelector('.handle'),sizes=[40,65,90];let start=null;function snap(value){const size=sizes.reduce((a,b)=>Math.abs(b-value)<Math.abs(a-value)?b:a);dialog.style.height=size+'%';dialog.dataset.snap=size;handle.setAttribute('aria-valuetext',size+'%');handle.setAttribute('aria-valuenow',size);document.querySelectorAll('[data-size]').forEach(b=>b.setAttribute('aria-pressed',String(Number(b.dataset.size)===size)))}snap(65);document.querySelectorAll('[data-size]').forEach(b=>b.onclick=()=>snap(Number(b.dataset.size)));handle.onpointerdown=e=>{start={y:e.clientY,size:dialog.getBoundingClientRect().height/innerHeight*100};handle.setPointerCapture(e.pointerId);dialog.style.transition='none'};handle.onpointermove=e=>{if(start)dialog.style.height=Math.max(30,Math.min(95,start.size+(start.y-e.clientY)/innerHeight*100))+'%'};function finish(){if(!start)return;start=null;dialog.style.transition='';snap(dialog.getBoundingClientRect().height/innerHeight*100)}handle.onpointerup=finish;handle.onpointercancel=finish;handle.onkeydown=e=>{if(['ArrowUp','ArrowDown','Home','End'].includes(e.key)){e.preventDefault();const i=sizes.indexOf(Number(dialog.dataset.snap));snap(e.key==='Home'?40:e.key==='End'?90:sizes[Math.max(0,Math.min(2,i+(e.key==='ArrowUp'?1:-1)))])}};document.querySelector('.reserve').onclick=()=>document.querySelector('.selection').textContent=document.querySelector('input:checked').value+' 선택됨 · 데모 예약';`,
  logic:'A bottom-anchored sheet uses native dialog. The passive initial preview does not steal catalog focus; the open button invokes showModal for focus containment, Escape/backdrop dismissal and trigger restoration. Pointer capture on the touch-action:none handle tracks height, then snaps to nearest 40/65/90% on release/cancel. Arrow keys and explicit snap buttons offer equivalent control. Inner content scrolls independently; selecting a time updates local confirmation.',
  acceptance:'Card and Detail visibly anchor to the bottom. Drag with mouse/touch and keyboard snaps change actual height; opening is modal, Escape restores trigger focus, inner scrolling and local time selection work. Must not substitute a centered static card.'
 },
 'pattern-command-palette': {
  html:`<div class="workspace"><small class="eyebrow">WORKSPACE / QUICK ACTIONS</small><h1>생각에서 실행으로.</h1><button class="open-dialog primary">명령 검색 <kbd>⌘ / Ctrl K</kbd></button><p class="result" role="status">명령을 선택해보세요.</p></div><dialog open aria-labelledby="command-title"><header><h2 id="command-title">빠른 명령</h2><button class="close" aria-label="닫기">×</button></header><input class="search" role="combobox" aria-label="명령 검색" aria-expanded="true" aria-autocomplete="list" aria-controls="commands" placeholder="검색 또는 명령 입력…" autocomplete="off"><div id="commands" role="listbox" aria-label="명령 목록"></div><footer>↑ ↓ 이동 <span>Enter 실행 · Esc 닫기</span></footer></dialog>`,
  css:base+`dialog{position:absolute;inset:10% 0 auto;margin:0 auto;width:min(88%,510px);padding:0;max-height:84%;display:flex;flex-direction:column}dialog header{display:flex;align-items:center;justify-content:space-between;padding:8px 14px}dialog h2{font-size:12px;margin:0}.close{border:0;min-height:28px;padding:3px 10px}.search{margin:0 12px 8px;padding:12px;border:1px solid #dbe1ec;border-radius:8px;min-width:0;font:13px system-ui;color:#202b42;background:#f7f8fc}#commands{overflow:auto;min-height:0;padding:0 8px 8px}.group-label{font:10px monospace;color:#6c7891;padding:8px}.command{display:flex;justify-content:space-between;gap:8px;border-radius:7px;padding:11px 10px;font-size:12px;cursor:pointer}.command[aria-selected=true]{background:#eef0ff;color:#3f4db9}.command small{font-size:10px;color:#78839b}.empty{padding:18px;font-size:12px;color:#6c7891}dialog footer{display:flex;justify-content:space-between;gap:8px;padding:10px 14px;border-top:1px solid #e5e8ef;font-size:10px;color:#74809a}.workspace.alternate{background:#d9e3d9}.result{margin-top:22px}kbd{margin-left:12px;font-size:10px}@container(max-width:440px){dialog{inset:7% 0 auto;max-height:88%;width:92%}dialog header{padding:4px 10px}.search{padding:8px;margin-bottom:4px}.command{padding:8px}.group-label{padding:5px}dialog footer{padding:7px 10px;font-size:9px}}`,
  js:modal+dialogFocusLoop+`const input=document.querySelector('.search'),list=document.querySelector('#commands'),actions=[{id:'new',group:'만들기',label:'새 프로젝트',keywords:'new project',result:'새 프로젝트 초안이 생성되었습니다.'},{id:'note',group:'만들기',label:'메모 작성',keywords:'write note',result:'새 메모를 시작합니다.'},{id:'recent',group:'이동',label:'최근 파일',keywords:'recent files',result:'최근 파일 3개를 표시합니다.'},{id:'team',group:'이동',label:'팀 작업 공간',keywords:'team workspace',result:'팀 작업 공간을 선택했습니다.'},{id:'theme',group:'설정',label:'테마 변경',keywords:'theme appearance',result:'화면 테마를 전환했습니다.'}];let filtered=actions,active=0;function match(text,q){let i=0;for(const ch of text.toLowerCase())if(ch===q[i])i++;return i===q.length}function draw(){list.replaceChildren();let group='';filtered.forEach((a,i)=>{if(a.group!==group){const heading=document.createElement('div');heading.className='group-label';heading.textContent=a.group;list.append(heading);group=a.group}const row=document.createElement('div');row.id='cmd-'+a.id;row.className='command';row.role='option';row.setAttribute('aria-selected',String(i===active));row.innerHTML='<span>'+a.label+'</span><small>↵</small>';row.onclick=()=>run(i);list.append(row)});if(!filtered.length){const empty=document.createElement('p');empty.className='empty';empty.textContent='일치하는 명령이 없습니다.';list.append(empty);input.removeAttribute('aria-activedescendant')}else input.setAttribute('aria-activedescendant','cmd-'+filtered[active].id)}function run(i){const a=filtered[i];if(!a)return;document.querySelector('.result').textContent=a.result;if(a.id==='theme')document.querySelector('.workspace').classList.toggle('alternate');dialog.close()}input.oninput=()=>{const q=input.value.toLowerCase().replace(/\\s/g,'');filtered=actions.filter(a=>match(a.label+a.group+a.keywords,q));active=0;draw()};input.onkeydown=e=>{if(['ArrowDown','ArrowUp','Home','End'].includes(e.key)&&filtered.length){e.preventDefault();active=e.key==='Home'?0:e.key==='End'?filtered.length-1:(active+(e.key==='ArrowDown'?1:-1)+filtered.length)%filtered.length;draw();document.getElementById('cmd-'+filtered[active].id).scrollIntoView({block:'nearest'})}else if(e.key==='Enter'){e.preventDefault();run(active)}};opener.onclick=()=>{openDialog();input.focus()};document.addEventListener('keydown',e=>{if((e.metaKey||e.ctrlKey)&&e.key.toLowerCase()==='k'){e.preventDefault();if(dialog.matches(':modal'))dialog.close();else{openDialog();input.focus()}}else if(e.key==='Escape'&&dialog.open)dialog.close()});draw();`,
  logic:'Native dialog enters modal mode on explicit open or Cmd/Ctrl+K. A combobox filters grouped actions with case-insensitive ordered-subsequence matching of labels and keywords. Display and keyboard order share one filtered array. ArrowUp/Down wrap, Home/End jump, Enter executes the active action, empty results clear aria-activedescendant. Escape/click dismissal restores the opener. All actions are local demo state.',
  acceptance:'Search changes real results, including empty state. Arrow navigation moves aria-selected and aria-activedescendant together; Enter announces the selected action. Explicit open contains focus. Must not render a decorative search field with static results.'
 },
 'pattern-toast': {
  html:`<div class="workspace"><small class="eyebrow">FEEDBACK / NON-BLOCKING</small><h1>작업의 흐름 그대로.</h1><p class="supporting">알림 위에 포인터나 키보드 포커스를 두면 닫힘이 잠시 멈춥니다.</p><div class="toolbar"><button class="primary" data-toast="success">저장 완료</button><button data-toast="error">오류 알림</button><button data-toast="info">공유 알림</button></div><p class="counter">0개의 알림</p></div><div class="announcer" role="status" aria-live="polite" aria-atomic="true"></div><section class="toasts" aria-label="알림 목록"></section>`,
  css:base+`.toasts{z-index:5;position:absolute;right:16px;bottom:16px;width:min(360px,calc(100% - 32px));display:flex;flex-direction:column;gap:8px}.toast{display:grid;grid-template-columns:28px 1fr 30px;gap:10px;align-items:center;border:1px solid #d9dfeb;border-radius:12px;background:white;box-shadow:0 8px 24px #22365a18;padding:12px;animation:toast-in 220ms ease-out}.toast .icon{border-radius:50%;width:26px;height:26px;display:grid;place-content:center;background:#e5f5ea;color:#22744c}.toast.error .icon{background:#ffe7e7;color:#a73142}.toast.info .icon{background:#e9edff;color:#4555b3}.toast strong{display:block;font-size:12px}.toast p{margin:4px 0 0;font-size:10px;color:#728097}.toast button{border:0;background:none;min-height:30px;color:#65718a;font-size:18px}.counter{font-size:11px!important}.announcer{position:absolute;width:1px;height:1px;overflow:hidden;clip-path:inset(50%)}@keyframes toast-in{from{opacity:0;transform:translateY(12px)}to{opacity:1;transform:none}}@container(max-width:440px){.toasts{right:10px;bottom:10px;width:calc(100% - 20px);gap:5px}.toast{padding:9px}.workspace .toolbar button{padding:8px;min-height:34px;font-size:10px}}`,
  js:`const stack=document.querySelector('.toasts'),messages={success:['✓','변경 사항을 저장했습니다','로컬 데모 · 저장 완료'],error:['!','저장하지 못했습니다','잠시 후 다시 시도해주세요'],info:['↗','공유 링크가 준비되었습니다','팀에 전달할 수 있습니다']};let entries=[];function count(){document.querySelector('.counter').textContent=entries.length+'개의 알림'}function remove(entry){clearTimeout(entry.timer);entry.el.remove();entries=entries.filter(x=>x!==entry);count()}function pause(entry){if(entry.timer){clearTimeout(entry.timer);entry.timer=null;entry.remaining=Math.max(0,entry.remaining-(performance.now()-entry.started))}}function resume(entry){if(entry.timer||document.hidden||entry.el.matches(':hover')||entry.el.contains(document.activeElement))return;entry.started=performance.now();entry.timer=setTimeout(()=>remove(entry),entry.remaining)}function add(type){if(entries.length===3)remove(entries[0]);const m=messages[type],el=document.createElement('article');el.className='toast '+type;el.innerHTML='<span class="icon" aria-hidden="true">'+m[0]+'</span><div><strong>'+m[1]+'</strong><p>'+m[2]+'</p></div><button aria-label="알림 닫기">×</button>';const entry={el,remaining:5000,timer:null,started:0};entries.push(entry);stack.append(el);el.querySelector('button').onclick=()=>remove(entry);el.onpointerenter=()=>pause(entry);el.onpointerleave=()=>resume(entry);el.onfocusin=()=>pause(entry);el.onfocusout=()=>queueMicrotask(()=>resume(entry));document.querySelector('.announcer').textContent=m[1];resume(entry);count()}document.querySelectorAll('[data-toast]').forEach(b=>b.onclick=()=>add(b.dataset.toast));document.addEventListener('visibilitychange',()=>entries.forEach(document.hidden?pause:resume));window.addEventListener('pagehide',()=>entries.forEach(e=>clearTimeout(e.timer)),{once:true});add('success');`,
  logic:'A non-blocking bottom-right stack holds at most three distinct notifications. Each has a 5000ms remaining-time budget: hover, focus-within and hidden document pause it, resuming only after all pause conditions clear. Manual dismissal removes only that entry. A separate polite live region announces new messages once. pagehide clears timers. No backend action is claimed.',
  acceptance:'Repeated actions build a capped stack with success/error/info states; manual close, timeout, hover pause and keyboard-focus pause work without blocking other actions. Must not show an immortal static status card.'
 },
 'pattern-skeleton': {
  html:`<div class="workspace"><header class="loading-header"><div><small class="eyebrow">LIBRARY / RECENT WORK</small><h1>나의 프로젝트</h1></div><div class="toolbar"><button class="reload">다시 불러오기</button><button class="fail">오류 보기</button></div></header><div class="project-list" aria-busy="true"></div><p class="loading-status" role="status">프로젝트를 불러오는 중…</p></div>`,
  css:base+`.loading-header{display:flex;justify-content:space-between;align-items:center;gap:10px}.loading-header h1{font-size:clamp(22px,4cqw,34px)}.loading-header .toolbar{flex-shrink:0}.project-list{display:flex;flex-direction:column;gap:10px}.project-row{height:74px;padding:12px;display:grid;grid-template-columns:48px 1fr 44px;align-items:center;gap:14px;border:1px solid #dce2ec;background:white;border-radius:12px}.thumb{width:48px;height:48px;border-radius:8px;display:grid;place-content:center;color:#fff;font:20px Georgia;background:#6572a1}.copy strong{display:block;font-size:13px;line-height:20px}.copy span{display:block;font-size:10px;line-height:18px;color:#758099}.project-row .tag{font-size:9px;color:#61708c}.skeleton{background:#e6e9ef;position:relative;overflow:hidden;color:transparent!important}.skeleton:after{content:'';position:absolute;inset:0;background:linear-gradient(90deg,transparent,#ffffffb0,transparent);animation:shimmer 1.5s infinite}.copy .skeleton:first-child{width:65%;height:14px;margin:3px 0 8px}.copy .skeleton:last-child{width:42%;height:10px}.tag.skeleton{height:12px}.error-panel{height:242px;display:grid;place-content:center;text-align:center;border:1px dashed #bd6e79;border-radius:12px;background:#fff7f7}.error-panel p{color:#983d4e}.loading-status{font-size:11px!important;margin:10px 0}@keyframes shimmer{from{transform:translateX(-100%)}to{transform:translateX(100%)}}@container(max-width:440px){.loading-header{align-items:flex-start}.loading-header h1{font-size:20px;margin:8px 0}.loading-header .toolbar{flex-direction:column;gap:4px;margin:0}.loading-header button{padding:5px 7px;min-height:28px;font-size:9px}.project-list{gap:7px;margin-top:10px}.project-row{height:58px;padding:8px;grid-template-columns:36px 1fr 32px;gap:10px}.thumb{width:36px;height:36px}.copy strong{font-size:11px}.copy span{font-size:9px}.error-panel{height:188px}}`,
  js:`const list=document.querySelector('.project-list'),status=document.querySelector('.loading-status'),data=[['F','Form study','오늘 수정됨 · 디자인','#6572a1'],['A','Archive 04','어제 수정됨 · 리서치','#b28165'],['N','Notes & ideas','2일 전 · 기획','#618572']];let timer;function load(fail=false){clearTimeout(timer);list.setAttribute('aria-busy','true');list.dataset.state='loading';status.textContent='프로젝트를 불러오는 중…';list.innerHTML=data.map(()=>'<article class="project-row" aria-hidden="true"><div class="thumb skeleton"></div><div class="copy"><strong class="skeleton"></strong><span class="skeleton"></span></div><span class="tag skeleton"></span></article>').join('');timer=setTimeout(()=>{list.setAttribute('aria-busy','false');list.dataset.state=fail?'error':'loaded';if(fail){list.innerHTML='<div class="error-panel"><p>프로젝트를 불러오지 못했습니다.</p><button class="retry">다시 시도</button></div>';list.querySelector('.retry').onclick=()=>load();status.textContent='연결 오류 · 로컬 데모'}else{list.innerHTML=data.map(d=>'<article class="project-row"><div class="thumb" style="background:'+d[3]+'">'+d[0]+'</div><div class="copy"><strong>'+d[1]+'</strong><span>'+d[2]+'</span></div><span class="tag">초안</span></article>').join('');status.textContent='프로젝트 3개를 불러왔습니다.'}},1200)}document.querySelector('.reload').onclick=()=>load();document.querySelector('.fail').onclick=()=>load(true);window.addEventListener('pagehide',()=>clearTimeout(timer),{once:true});load();`,
  logic:'Three structural skeleton rows reserve exactly the loaded thumbnail/text/tag geometry. A cancellable 1200ms local load resolves to content or an explicit error with retry. aria-busy spans the request; skeletons are hidden from assistive technology while a live status announces progress/outcome. Reload cancels the previous timer. Reduced motion removes shimmer without changing loading state.',
  acceptance:'Loading resolves into actual content with unchanged row bounds, error and retry are reachable, rapid reload cannot race older responses. Must not leave an endless decorative shimmer or replace the whole layout with a spinner.'
 },
 'pattern-segmented': {
  html:`<div class="workspace"><small class="eyebrow">ANALYTICS / FOCUS TIME</small><header class="seg-header"><h1>꾸준함의 기록</h1><fieldset class="segments"><legend>집중 시간 기간</legend><i class="indicator" aria-hidden="true"></i>${['주간','월간','연간'].map((s,i)=>`<label><input type="radio" name="period" value="${i}" ${i===0?'checked':''}><span>${s}</span></label>`).join('')}</fieldset></header><section class="report" aria-label="집중 시간"><div class="metric"><strong>24.5</strong><span>시간</span><small>이번 주 · +12%</small></div><div class="chart" aria-label="기간별 집중 시간"></div><p class="period-description" role="status"></p></section></div>`,
  css:base+`.seg-header{display:flex;align-items:center;justify-content:space-between;gap:18px;flex-wrap:wrap}.segments{position:relative;display:flex;padding:4px;background:#dfe4ef;border:1px solid #d1d8e7;border-radius:10px;margin:0;min-width:220px}.segments legend{position:absolute;width:1px;height:1px;overflow:hidden;clip-path:inset(50%)}.segments label{flex:1;position:relative;text-align:center;cursor:pointer}.segments input{position:absolute;opacity:0;width:100%;height:100%;margin:0;inset:0;cursor:pointer}.segments span{display:block;min-height:36px;padding:10px 14px;font-size:12px;position:relative;pointer-events:none}.segments input:focus-visible+span{outline:3px solid #596bdd;outline-offset:1px;border-radius:7px}.segments input:checked+span{color:#394db9;font-weight:650}.indicator{position:absolute;top:4px;height:36px;background:white;border-radius:7px;box-shadow:0 2px 5px #34456919;transition:left 200ms,width 200ms}.report{background:white;border:1px solid #dbe1ec;border-radius:18px;margin-top:22px;padding:clamp(16px,3cqw,30px)}.metric{display:flex;align-items:baseline;gap:8px}.metric strong{font-size:clamp(30px,6cqw,54px);letter-spacing:-.05em}.metric span{font-size:12px;color:#697790}.metric small{margin-left:auto;color:#437a61;font-size:10px}.chart{height:clamp(65px,20cqw,180px);margin-top:22px;display:flex;align-items:flex-end;gap:clamp(8px,3cqw,28px);border-bottom:1px solid #dce2ec;padding:0 10px}.bar{flex:1;height:var(--value);background:#8290df;border-radius:5px 5px 0 0;position:relative;transition:height 220ms}.bar:nth-child(even){background:#5267c9}.period-description{margin:16px 0 0!important;font-size:11px!important}@container(max-width:440px){.seg-header{gap:8px}.seg-header h1{font-size:22px;margin:8px 0}.segments{min-width:180px}.segments span{min-height:30px;padding:7px 10px;font-size:11px}.indicator{height:30px}.report{margin-top:12px;padding:12px}.chart{margin-top:10px;height:65px}.metric strong{font-size:30px}.period-description{margin-top:8px!important}}`,
  js:`const radios=[...document.querySelectorAll('[name=period]')],indicator=document.querySelector('.indicator'),periods=[{total:'24.5',label:'이번 주 · +12%',description:'월–일, 일별 집중 시간을 표시합니다.',values:[40,68,52,88,74,35,60]},{total:'102',label:'이번 달 · +8%',description:'1–4주, 주별 집중 시간을 표시합니다.',values:[55,72,66,92]},{total:'1,248',label:'올해 · +18%',description:'1–12월, 월별 집중 시간을 표시합니다.',values:[30,38,50,44,65,72,68,80,74,88,82,96]}];function position(){const label=radios.find(r=>r.checked).parentElement;indicator.style.left=label.offsetLeft+'px';indicator.style.width=label.offsetWidth+'px'}function update(){const index=Number(radios.find(r=>r.checked).value),p=periods[index];document.querySelector('.metric strong').textContent=p.total;document.querySelector('.metric small').textContent=p.label;document.querySelector('.period-description').textContent=p.description;const chart=document.querySelector('.chart');chart.innerHTML=p.values.map((v,i)=>'<div class="bar" style="--value:'+v+'%" role="img" aria-label="'+(i+1)+'구간: '+v+'"></div>').join('');chart.dataset.period=index;position()}radios.forEach(r=>r.onchange=update);document.querySelector('.segments').addEventListener('keydown',e=>{if(e.key==='Home'||e.key==='End'){e.preventDefault();const r=radios[e.key==='Home'?0:2];r.checked=true;r.focus();update()}});const observer=new ResizeObserver(position);observer.observe(document.querySelector('.segments'));window.addEventListener('pagehide',()=>observer.disconnect(),{once:true});update();`,
  logic:'One native radio group selects mutually exclusive week/month/year data. Native arrows wrap the selection; Home/End select endpoints. The selected label geometry drives a measured sliding indicator through ResizeObserver. Selection updates total, comparison, chart bar count and accessible period description together. Reduced motion makes indicator/bar transitions immediate.',
  acceptance:'Exactly one radio stays checked; click, touch and arrow keys update the indicator AND corresponding report. Resizing preserves alignment. Must not implement unrelated navigation tabs or a cosmetic moving pill over unchanged content.'
 }
}
